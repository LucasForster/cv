\ProvidesPackage{preamble}

% page layout
\geometry{left=1.25cm,right=1.25cm,top=1.5cm,bottom=1.5cm,columnsep=1.2cm}

% packages
\usepackage{paracol} % The paracol package lets you typeset columns of text in parallel

\usepackage{array}
\usepackage{calc}
\usepackage[showisoZ=true,datesep=/,showseconds=false,showisoZ=false,showzoneminutes=false]{datetime2}
\usepackage{worldflags}

% font
\ifxetexorluatex
  % If using xelatex or lualatex:
  \setmainfont{Roboto Slab}
  \setsansfont{Lato}
  \renewcommand{\familydefault}{\sfdefault}
\else
  % If using pdflatex:
  \usepackage[rm]{roboto}
  \usepackage[defaultsans]{lato}
  % \usepackage{sourcesanspro}
  \renewcommand{\familydefault}{\sfdefault}
\fi

\renewcommand{\namefont}{\Huge\rmfamily\bfseries}
\renewcommand{\taglinefont}{\Large\bfseries}
\renewcommand{\personalinfofont}{\normalsize\bfseries} % \footnotesize
\renewcommand{\cvsectionfont}{\LARGE\rmfamily\bfseries}
\renewcommand{\cvsubsectionfont}{\large\bfseries}

% colors
\definecolor{SlateGrey}{HTML}{2E2E2E}
\definecolor{LightGrey}{HTML}{666666}
\definecolor{DarkPastelRed}{HTML}{450808}
\definecolor{PastelRed}{HTML}{8F0D0D}
\definecolor{GoldenEarth}{HTML}{E7D192}
% https://color.adobe.com/de/Vitamin-C-color-theme-492199/
\definecolor{ThemeBlue}{HTML}{004358}
\definecolor{ThemeGreen}{HTML}{1f8a70}
\definecolor{ThemeLime}{HTML}{bedb39}
\definecolor{ThemeYellow}{HTML}{ffe11a}
\definecolor{ThemeOrange}{HTML}{fd7400}

\colorlet{name}{ThemeBlue} % black
\colorlet{tagline}{ThemeOrange} % PastelRed
\colorlet{heading}{ThemeBlue} % DarkPastelRed
\colorlet{headingrule}{ThemeYellow} % GoldenEarth
\colorlet{subheading}{ThemeOrange} % PastelRed
\colorlet{accent}{ThemeOrange} % PastelRed
\colorlet{emphasis}{SlateGrey}
\colorlet{body}{LightGrey}

% bullets, markers
\renewcommand{\itemmarker}{{\small\textbullet}}
\renewcommand{\ratingmarker}{\faCircle}

% graphics
\graphicspath{{graphics/}}

% commands
\newcommand{\link}[1]{
  \color{accent}\faLink\hspace{.5ex}\color{body}
  \begin{minipage}{\columnwidth-\widthof{\faLink\hspace{.5ex}}}
    \href{https://#1}{#1}
  \end{minipage}
} % \NewInfoField{link}{\faLink}[https://]
\newcommand{\yearRight}[1]{\hfill\small\color{body}\faCalendar~#1}
\newcommand{\header}[2]{%
  % copied from \cvevent definition, changed skips and removed #3, #4
  {\large\color{emphasis}#1\par}
  \normalsize
  \ifstrequal{#2}{}{}{
    \smallskip
    \textbf{\color{accent}#2}\par
  }
  \medskip
}

\newcommand{\subtitle}[1]{{\color{emphasis}#1\par}}

% languages
\newcommand{\en}[1]{}
\newcommand{\de}[1]{}
